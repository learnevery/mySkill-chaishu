# 第0步：初始化与偏好加载

**每次触发SKILL时首先执行：**

## 1. 读取用户偏好

检查 `user-preferences.json` 是否存在
- 存在 → 加载偏好数据，用于后续选项排序、推荐标记、个性化欢迎语
- 不存在 → 使用默认值，首次交互结束后自动创建

偏好数据结构与更新规则详见 → [shared-infrastructure.md](shared-infrastructure.md)

## 1.5 检测拆书知识库

按 [kb-integration.md](kb-integration.md) 的位置约定检测 chaishu 知识库文件（`人设.md` / `剧情.md` / `爽点.md` / `节奏.md` / `金手指.md` / `范式.md`）：

- **存在** → 统计积累规模（拆过几本书、覆盖哪些赛道），记入会话状态供后续阶段使用；在欢迎语中一并报告："检测到拆书知识库（X 本书积累，覆盖 [赛道列表]），本次创作将套用已验证的范式"
- **不存在** → 跳过，后续阶段使用内置通用指南，不做任何提示（避免打扰未使用 chaishu 的用户）

## 2. 快捷入口检测

分析用户的首次输入是否已包含丰富的创作信息（题材、主角、冲突等关键要素）

- 如果信息充足 → 展示提取结果，提供三个选项：
  - "是的，直接开始规划" → 跳过问答，进入第二阶段
  - "先展示提取的信息让我确认/修改" → 展示配置供确认后进入规划
  - "不，我还是想走一遍问答流程" → 进入第一层问答
- 如果信息不足 → 直接进入第一层问答

## 3. 个性化欢迎

（如有偏好数据）：

> "欢迎回来！基于你之前的创作习惯，我为你做了一些推荐（标记为⭐的选项）。"

## 4. 中断续写检测

扫描 `./小说/` 目录下的项目文件夹：

- 查找含 `02-写作计划.json` 且 `status` 为 `"in_progress"` 或 `"validating"` 的项目
- 如果找到未完成项目：
  - 展示项目信息：小说名称、完成进度（X/Y 章已完成）
  - 提供选项：
    - "继续上次创作" → 直接进入第三阶段，从中断章节继续
    - "开始新作品" → 进入第一层问答
- 如果未找到 → 正常进入快捷入口检测或第一层问答
